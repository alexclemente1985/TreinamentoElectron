# Informações importantes

## Instalação electron
- `npm install --save electron`

## Instalação ffmpeg
1) Instalar `fluent-ffmpeg`: `npm install --save fluent-ffmpeg`
2) Instalar `ffmpeg` no computador: `brew install ffmpeg` **OSX**